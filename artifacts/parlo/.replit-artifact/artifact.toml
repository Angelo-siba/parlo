kind = "web"
previewPath = "/"
title = "Parlo"
version = "1.0.0"
id = "artifacts/parlo"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24762

[services.development]
run = "pnpm --filter @workspace/parlo run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/parlo", "run", "build" ]
publicDir = "artifacts/parlo/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24762"
BASE_PATH = "/"
